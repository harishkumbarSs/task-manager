# Copy to `.env` in the same folder. Used by Vite at **build time** for production.

# Public API base URL (no trailing slash). Example: https://task-manager-api.onrender.com
# Leave empty for local dev — Vite proxy forwards `/api` to the backend.
VITE_API_URL=
